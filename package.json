{
  "name": "waterbim",
  "version": "1.0.0",
  "description": "WaterBIM: K-water 상하수도 BIM code/color-standard search, term dictionary, and BIM task-classification workflow (static HTML, no server needed)",
  "private": true,
  "scripts": {
    "extract": "node scripts/xlsx2json.js",
    "build-index": "node scripts/build-index.js",
    "build-html": "node scripts/build-html.js",
    "build-artifact": "node scripts/build-artifact.js",
    "build": "npm run build-index && npm run build-html && npm run build-artifact"
  }
}
